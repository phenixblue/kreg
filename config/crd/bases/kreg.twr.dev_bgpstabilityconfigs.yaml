---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: bgpstabilityconfigs.kreg.twr.dev
spec:
  group: kreg.twr.dev
  names:
    kind: BGPStabilityConfig
    listKind: BGPStabilityConfigList
    plural: bgpstabilityconfigs
    singular: bgpstabilityconfig
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BGPStabilityConfig is the Schema for the bgpstabilityconfigs
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of BGPStabilityConfig
            properties:
              additionDelay:
                description: |-
                  additionDelay is how long a newly-advertised route must be settled
                  before it's added as a backend.
                type: string
              dampening:
                description: |-
                  DampeningConfig configures flap dampening: an exponentially-decaying
                  instability score accumulates on each flap and decays with halfLife;
                  crossing suppressThreshold suppresses the backend until the score
                  decays back below reuseThreshold, capped by maxSuppress. The specific
                  algorithm sits behind an internal interface (internal/damp.Damper) and
                  may change; these fields describe its tunable behavior, not its
                  implementation.
                properties:
                  enabled:
                    type: boolean
                  halfLife:
                    type: string
                  maxSuppress:
                    type: string
                  reuseThreshold:
                    format: int32
                    type: integer
                  suppressThreshold:
                    format: int32
                    type: integer
                type: object
              withdrawalGrace:
                description: |-
                  withdrawalGrace is the hold-down before removing a backend that
                  stopped being advertised.
                type: string
            type: object
          status:
            description: status defines the observed state of BGPStabilityConfig
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the BGPStabilityConfig
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
